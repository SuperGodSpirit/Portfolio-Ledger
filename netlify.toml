[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Firebase web API keys are intentionally public identifiers.
  SECRETS_SCAN_OMIT_PATHS = "public/firebase-messaging-sw.js"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
